# Orca 运行的独立 namespace，所有资源（SA / ConfigMap / Deployment / Service）都放在这里。
apiVersion: v1
kind: Namespace
metadata:
  name: orca-system
  labels:
    app.kubernetes.io/name: orca
    app.kubernetes.io/part-of: orca
