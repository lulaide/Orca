# Orca 敏感配置。
# db_password 默认值 "orca"，生产环境请替换。
# LLM API Key 不需要预配 — 部署后在 Web Settings 页面配置即可。
apiVersion: v1
kind: Secret
metadata:
  name: orca-secrets
  namespace: orca-system
  labels:
    app.kubernetes.io/name: orca
type: Opaque
data:
  # echo -n "orca" | base64
  db_password: b3JjYQ==
